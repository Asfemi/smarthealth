import 'package:flutter/material.dart';

class Devices extends StatelessWidget {
  static String id = 'devices';
  //const Settings({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Scaffold(
        appBar: AppBar(),
        body: Container(),
      ),
    );
  }
}
